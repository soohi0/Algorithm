//1018 체스판 다시 칠하기
#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main() {

	vector <string> sv;
	int N, M = 0;

	cin >> N >> M;//N 세로, M 가로

	for (int i = 0; i < N; i++) {
		sv.
			for (int j = 0; j < M; j++) {
				char ch;
				cin >> ch;

			}
	}

	return 0;
}